/** @type {import('next').NextConfig} */

// This is the "Master Control" file for your entire website
const nextConfig = {
  /* This is where you can add special rules for things like images, 
     security headers, or custom redirects in the future */
};

// This sends the configuration out so the project can use it to build the site
export default nextConfig;
